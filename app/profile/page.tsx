"use client";import {useEffect,useState} from "react";export default function Profile(){const [u,setU]=useState<any>();useEffect(()=>{fetch('/api/me').then(r=>r.json()).then(x=>setU(x.user))},[]);if(!u)return <main className="center">Memuat...</main>;async function save(e:any){e.preventDefault();const body=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/profile',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify(body)});alert(r.ok?'Profil disimpan':'Gagal')}return <main><header><a href="/" className="brand">&lt;/&gt; CodeSphere</a></header><section className="profile"><img className="avatarbig" src={u.avatar_url||`https://api.dicebear.com/9.x/initials/svg?seed=${u.username}`}/><h1>{u.name} {u.verified&&<span className="check">✓</span>}</h1><p>@{u.username}</p><form onSubmit={save}><label>Nama<input name="name" defaultValue={u.name}/></label><label>URL foto profil<input name="avatar_url" defaultValue={u.avatar_url} placeholder="https://..."/></label><label>Bio<textarea name="bio" defaultValue={u.bio}/></label><button>Simpan profil</button></form><div className="requests"><h2>Request masuk</h2><RequestList/></div></section></main>};function RequestList(){const [r,setR]=useState<any[]>([]);useEffect(()=>{fetch('/api/requests').then(x=>x.json()).then(setR)},[]);return <>{r.map(x=><div className="request" key={x.id}><strong>{x.name} (@{x.username})</strong><p>{x.message}</p></div>)}{!r.length&&<p>Belum ada request.</p>}</>}
