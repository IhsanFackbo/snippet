import {NextResponse} from "next/server";import {requireSession} from "@/lib/auth";import {db,ensureSchema} from "@/lib/db";export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){try{await ensureSchema();const s=await requireSession();if(s.role!=="owner")throw new Error();const {id}=await params;await db().query("UPDATE users SET verified=NOT verified WHERE id=$1",[id]);return NextResponse.json({ok:true})}catch{return NextResponse.json({error:"Khusus pemilik"},{status:403})}}
