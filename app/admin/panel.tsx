"use client";export default function Panel({users}:{users:any[]}){return <main><header><a href="/" className="brand">&lt;/&gt; CodeSphere</a></header><section className="admin"><h1>Kendali Pemilik</h1><p>Hanya pemilik web yang dapat memberi atau mencabut centang biru.</p>{users.map(u=><div className="userrow" key={u.id}><div><strong>{u.name} {u.verified&&<span className="check">✓</span>}</strong><small>@{u.username} · {u.email} · {u.role}</small></div><button onClick={async()=>{await fetch(`/api/admin/users/${u.id}/verify`,{method:'POST'});location.reload()}}>{u.verified?'Cabut centang':'Beri centang'}</button></div>)}</section></main>}
